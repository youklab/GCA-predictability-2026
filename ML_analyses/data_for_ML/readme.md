# data_for_ML

This folder contains the raw simulation data and the frozen train/validation/test splits used for the machine-learning analysis in Koopmans, Kay, and Youk (2026). The data are organized into two subfolders.

---

## labeled_data_2025_parallel/

**Contents:** 100 compressed NumPy archive files:
```
v251209_chunk_0000.npz
v251209_chunk_0001.npz
...
v251209_chunk_0099.npz
```

**What each file contains:**
Each `.npz` file stores one batch of GCA simulations and contains two arrays:

| Array | Shape | dtype | Description |
|---|---|---|---|
| `data` | (N, 196) | int8 | Flattened initial configurations; each row is one 14 × 14 lattice, cell states ∈ {1, 2, 3, 4} |
| `labels` | (N,) | int8 | Binary outcome label: 1 = static configuration, 0 = dynamic pattern (rectilinear or spiral wave) |

Together, the 100 chunks contain **1,000,000 independent GCA simulations**, each started from a randomly drawn initial configuration on a 14 × 14 triangular lattice. Each chunk contains exactly 10,000 simulations.

**Class balance:**
We found that the dataset is class-imbalanced: static outcomes (label = 1) comprise approximately **26.7%** of the dataset, while dynamic outcomes (label = 0) comprise the remaining **73.3%**. This fraction is consistent across all 100 chunks (range: 25.5%–27.7%), confirming that the class distribution is stable across the simulation ensemble. This imbalance means that a classifier predicting "dynamic" for every example would achieve ~73% raw accuracy while providing no predictive information — motivating our use of balanced accuracy and ROC–AUC as primary metrics throughout the ML analysis. Per-chunk label fractions are documented in `ML_splits/chunk_label_fractions.csv` and `ML_splits/chunk_label_stats.json`.

---

## ML_splits/

**Contents:** The frozen train/validation/test split derived from the 100 data chunks.

| File | Description |
|---|---|
| `split_metadata.json` | Full split provenance: random seed, split sizes, chunk file list |
| `chunk_label_fractions.csv` | Per-chunk class balance diagnostics |
| `chunk_label_stats.json` | Overall class balance summary statistics |
| `test_indices.npy` | Global indices of the 100,000 frozen test examples |
| `val_indices.npy` | Global indices of the 50,000 validation examples |
| `train_indices.npy` | Global indices of the training pool (~850,000 examples) |
| `X_test.npy` | Materialized test set inputs; shape (100000, 196), dtype int8 |
| `y_test.npy` | Materialized test set labels; shape (100000,), dtype int8 |
| `X_val.npy` | Materialized validation set inputs; shape (50000, 196), dtype int8 |
| `y_val.npy` | Materialized validation set labels; shape (50000,), dtype int8 |
| `X_train_pool.npy` | Materialized full training pool; shape (850000, 196), dtype int8 |
| `y_train_pool.npy` | Materialized training pool labels; shape (850000,), dtype int8 |

**How the split was constructed:**
The split was generated by `prepare_splits_from_chunks.py` using a fixed random seed (12345) to shuffle the global index space across all 1,000,000 simulations before carving out the test and validation sets. The seed is documented in `split_metadata.json` and is fixed for the paper. The test set was frozen at the start of the analysis and **never used for model selection or threshold calibration** — all such decisions were made using the validation set only.

**Reproducing the split:**
The full split can be reproduced exactly from the raw chunk files by running `prepare_splits_from_chunks.py` followed by `materialize_train_pool_v3.py` with the settings recorded in `split_metadata.json`. The pre-materialized `.npy` files are provided here for convenience.

---

## Data citation

If you use these data, please cite:

> Koopmans, Kay, and Youk (2026).

